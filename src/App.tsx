function App() {
  // const { confirm } = useConfirm();
  return (
    <>
      {/*<MyPagination onPageChange={console.log} currentPage={1} totalPages={10000} />*/}
    </>
  );
}

export default App;
